{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "java",
			"targetPath": "${workspaceFolder}/${workspaceFolderBasename}.jar",
			"elements": [
				"${compileOutput:product-catalog}",
				"${dependencies:product-catalog}",
				"${compileOutput:shopping-cart}",
				"${dependencies:shopping-cart}"
			],
			"problemMatcher": [],
			"label": "java: exportjar:microsservicos-spring-cloud",
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}